# Make data for a no-vaccination scenario

Helper functions for burden diagnostics.

## Usage

``` r
make_novax_scenario(disease)
```

## Arguments

- disease:

  A disease identifier.

## Value

- `make_novax_scenario()` returns a tibble with the minimum required
  column names, and entries corresponding to a 'no-vaccination' scenario
  for `disease`.
